Contexto: Proyecto Rust (egui 0.29 + rusqlite + SQLite) de ~2680 líneas en 18 archivos. App de escritorio para reportes de contrataciones con UI immediate-mode, filtros dinámicos, exportación Excel/PDF/PPTX, y redactor de plantillas.

No intentes compilar ni instalar Rust toolchain. Entorno con solo 504MB de rootfs, insuficiente para rustc (~500MB). No ejecutes make build, cargo build, rustup, ni nada de compilación.

Rol: Auditor de código estático. Solo lectura de archivos fuente.

Reglas:
1. No corras comandos de build/compilación
2. No instales Rust
3. Lee los .rs directamente
4. Tus respuestas son análisis y sugerencias

Nota importante: Este proyecto ya pasó por dos rondas de auditoría con fixes aplicados:

Primera ronda (fixes aplicados):
- LIKE interpolation → bound parameters (analysis.rs)
- cat_/id_/preferred names hardcodeados → AnalyseConfig configurable via env (CATALOG_PREFIX, FK_ID_PREFIX, PREFERRED_NAME_COLS)
- vw_reporte_excel_contrataciones hardcodeado → eliminado (schema.rs)
- .unwrap_or(0) en queries → ? propagation (dashboard.rs)
- Analizar_columna ahora recibe &AnalyseConfig

Segunda ronda (fixes aplicados):
- "id" fallback en construir_mapeo_dependencia → detectar_pk_columna() con fallback a rowid
- starts_with("id") y "created_at","updated_at" en detectar_columna_nombre → AnalyseConfig (EXCLUDE_ID_PREFIX, EXCLUDE_NAME_COLS)
- Literales 25, 50, 0.8 en status detection → constantes (STATUS_SHORT_LENGTH_THRESHOLD, MAX_CATEGORICAL_VALUES, STATUS_SHORT_RATIO_THRESHOLD)
- 500 literal en export.rs → constants::TABLE_LIMIT

Tercera ronda (refactors aplicados):
- construir_where extraído en 5 funciones privadas (una por tipo de filtro)
- seleccionar_tabla dividido en clear_selection + load_and_analyse_table
- create_dir_all duplicado extraído a helper ensure_dir

Cuarta ronda (fixes residuales finales):
- unwrap() en app.rs y dashboard.rs reemplazados por pattern matching
- Logging en unwrap_or de analysis.rs para trazabilidad de errores
- view_keywords agregado a AnalyseConfig (VIEW_KEYWORDS env var)
- "reporte","excel","vw_" literal en schema.rs y explorer.rs ahora configurables
- MIN_FK_COUNT_FOR_OPTIMIZATION constante en lugar de literal 3

Busca específicamente hallazgos que NO se hayan cubierto, priorizando:

1. Bugs/code smells:
   - ¿Queda algún unwrap/expect/panic sin justificar?
   - Manejo de errores silencioso todavía presente?
   - Lógica frágil o difícil de seguir?

2. Seguridad:
   - SQL injection ya mitigado, ¿hay otros vectores?
   - Validación de input del usuario (file dialog, redactor, text_search)
   - Path traversal o permisos en exportación?

3. Refactor:
   - dashboard() función principal (~165 líneas) — ¿demasiado larga?
   - Código duplicado entre módulos?
   - Funciones con demasiados parámetros?

4. Cero hardcodeo:
   - ¿Quedan thresholds numéricos sin constante en db/constants.rs?
   - ¿Strings de schema hardcodeados?
   - ¿Assumptions sobre nombres de columnas/tablas?

Archivos principales:
- src/main.rs — entry point
- src/app.rs (~490 lines) — estado global, UI panels, tema Nord Light
- src/config.rs — detección de rutas, env vars para AnalyseConfig
- src/export.rs — exportación Excel/PDF/PPTX con screenshot
- src/redactor.rs — plantillas de texto con placeholders
- src/db/ — capa universal SQLite (8 archivos: mod, types, schema, analysis, dashboard, explorer, constants, utils)
- src/ui/ — UI panels (6 archivos: sidebar, tabla, charts, redactor_window, theme, widgets)

Comenzar: Dame un overview identificando bugs, code smells, problemas de seguridad residuales, y violaciones al principio de "cero hardcodeo" que aún persistan.
